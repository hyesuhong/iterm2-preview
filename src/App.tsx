import { Header } from '@/components/header';

function App() {
	return (
		<>
			<Header />
			<main>
				<section>color list</section>
				<section>preview</section>
			</main>
		</>
	);
}

export default App;
